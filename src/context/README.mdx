# Context Directory

This is where you would keep contexts that are used in different places throughout your application. In this example, maybe a context that would handle all of the CRUD operations for Todos and expose just the methods you would need to manipulate them.
